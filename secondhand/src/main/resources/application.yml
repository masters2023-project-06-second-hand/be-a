spring:
  profiles:
    group:
      local: localDB,jpa,aws,oauth,jwt,redis
      test: localDB,jpa,aws,oauth,jwt,redis
    active: local
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
